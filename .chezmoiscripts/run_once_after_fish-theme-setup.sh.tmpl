{{- if eq .chezmoi.os "windows" -}}
{{- else -}}
#!/bin/bash
set -euo pipefail

# fish themes are stored in universal variables, so this only needs to run once.
if command -v fish >/dev/null 2>&1; then
  fish -c 'fish_config theme choose Dracula'
fi
{{- end -}}
