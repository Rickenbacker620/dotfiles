**/.github/
**/*.md
**/LICENSE
**/screenshot.png

{{- if not (and (eq .chezmoi.os "linux") .hasDesktop) }}
# Desktop/GUI configs - only needed on Linux with GUI packages installed
.config/hypr/**
.config/waybar/**
.config/wofi/**
.config/wallpaper/**
.config/mpv/**
.config/fish/conf.d/hyprland.fish
{{- end }}

{{- if or (eq .chezmoi.os "windows") (and (eq .chezmoi.os "linux") (not .hasDesktop)) }}
# ghostty isn't installed on Windows, or on Linux without GUI packages
.config/ghostty/**
{{- end }}

{{- if eq .chezmoi.os "windows" }}
# Unix-only configs - not needed on Windows
.config/fish/**
.config/zellij/**
.bashrc
.zshrc
{{- else }}
# PowerShell 7+ profile - Windows only
Documents/PowerShell/**
{{- end }}