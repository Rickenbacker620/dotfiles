linux-arch:
  packageManager:
    update: "sudo pacman -Syu --noconfirm"
    install: "sudo pacman -S --noconfirm --needed"
  packages:
    essential:
      - openssh
      - 7zip
      - base-devel
      - git
      - wget
    desktop:
